.tmp_codex_key
.tmp_codex_key.pub
.ssh_config_codex
.codex
lighthouse-*.report.*
lighthouse-*.json
lighthouse-*.html

# Local-only automation workspace (not part of this repo)
company-os/

# Git worktrees created by company-os agent-runner (local machine)
.worktrees/

# Temporary worktrees for manual merge-conflict resolution
.mergefix/

# company-os Cursor agent scaffolding (never ship in PRs)
.company-os-task.json
TODO-NOTE.md

# Node artifacts (never commit)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Root `package.json` is intentional (thin scripts, e.g. `download:aab` → mobile-app).
# Do not add a competing app `package.json` at repo root without updating this policy.

# Local Search Console / Google tooling dumps (never commit secrets)
gsc.json

# Local Printful / catalog scan scratch (never commit)
star-map-app-final/.tmp-*

# Local-only drafts (PowerShell helpers, etc.; may contain secrets)
/drafts/
