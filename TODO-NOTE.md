## Measurement hooks (homepage)

Stable attributes for headless checks: `data-capture="promo-inline"` on the homepage promo email submit control (`PromotionSignup`), and `data-capture="promo-jump"` on the hero “Start free preview” link that targets `#preview` (jump into the preview funnel without the email step).
