## PR note (task 0ecdfbf3-99e8-49b0-a4e2-f334f3c3b69e)

Documentation only: interprets `npm run merchant:products:status` output (PASS/WARN/FAIL/INFO) as green/yellow/red and lists the next operator action per check. Wording matches the behavior of `scripts/merchant-products-status.mjs` on the branch that ships that command; this worktree may not include the script until merged.
