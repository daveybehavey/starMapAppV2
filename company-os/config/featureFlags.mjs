// Centralized feature flags for company-os (ops defaults)
// Keep BULK_EVENT_ORDERS_ENABLED off by default for bulk lane dark launch.
export const BULK_EVENT_ORDERS_ENABLED = false;

export default { BULK_EVENT_ORDERS_ENABLED };

